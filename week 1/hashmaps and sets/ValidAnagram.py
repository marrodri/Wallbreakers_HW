

# incomplete ask for help later
class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
        i = 0
        j = 0
        s_list  = str(s)
        t_list  = str(t)
        s_dict = {}
        t_dict = {}
        for char in s_list:
            if char.isalpha():
                if ():
        # one dictionary for the first string
        # check the second string with the dictionary, and decrease by one
        # for each character found,
        