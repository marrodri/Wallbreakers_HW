

# incomplete

class Solution:
    def longestCommonPrefix(self, strs: List[str]) -> str:
        i = len(strs)
        long_str = max(strs, key=len)
        arr_len = len(long_str)
        while ()
        